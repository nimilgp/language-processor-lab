%{
  //imports and variable declarations
%}

  // alias can be given for patterns(usefull for long patterns)

%%
  // alias/pattern  =>  action(c-code) 

%%
  //declare int yywrap() {} to avoid err or just use -ll -lfl during compilation

  //declare main function with yylex() called in it
